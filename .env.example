# Vercel / local frontend URLs
# No secrets here — only public API base URLs.

# Local (ng serve uses environment.ts):
# VITE-style not used; Angular reads src/environments/environment.ts

# Production: edit src/environments/environment.prod.ts before deploy, or replace at CI:
# apiUrl=https://YOUR_RENDER_API.onrender.com/api
# mediaBaseUrl=https://YOUR_RENDER_API.onrender.com
